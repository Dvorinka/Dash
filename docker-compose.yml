services:
  dash:
    # Prebuilt release image — `docker compose pull && docker compose up -d`.
    # To build from source instead: `docker compose up -d --build`.
    image: ghcr.io/dvorinka/dash:latest
    build:
      context: .
      dockerfile: infra/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    restart: unless-stopped
